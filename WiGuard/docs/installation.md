# Installation

1. `git clone ... && cd WiGuard`
2. `pip install -r requirements.txt`
3. `pip install .`
4. Run with admin/root privileges: `sudo wiguard scan` (Linux) or Administrator shell (Windows)
